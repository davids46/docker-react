language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t davids46/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true davids46/docker-react yarn test
